Kiểm tra kết nối DB php artisan migrate:status => Nếu hiện danh sách migration hoặc báo "no migrations found" → kết nối DB OK
php artisan migrate:install
php artisan migrate:status

# chạy lệnh

php artisan serve

php artisan session:table
php artisan migrate
